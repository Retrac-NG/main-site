import React from 'react';

const AppLayout = ({ children }) => {
  return (
    <div>
      {/* ====== Navbar */}
      {children}
      {/* ====== footer */}
    </div>
  );
};

export default AppLayout;
